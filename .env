# Docker Socket Configuration
# For rootless Docker, set DOCKER_SOCK to your user's Docker socket path
# Example for rootless Docker (replace 1000 with your UID):
# DOCKER_SOCK=/run/user/1000/docker.sock

# For standard Docker (default if not set):
# DOCKER_SOCK=/var/run/docker.sock

# Fake Traffic Generator Configuration
# Port mappings (internal container ports to host ports)
TRAFFIC_GENERATOR_PORT=9001
USER_DATABASE_PORT=9500
SERVER_ASSIGNMENT_PORT=9600

# Network configuration
# The fake-traffic-generator services will join the monitoring network
NETWORK_NAME=monitoring

# Configuration files
USER_FLOWS_FILE=./user_flows.yml
GEO_SERVERS_FILE=./geo_servers.geojson

# Logging configuration (GELF driver for Promtail)
LOGGING_DRIVER=gelf
LOGGING_GELF_ADDRESS=udp://promtail:12201
