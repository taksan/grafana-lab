global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'traffic-generator-monitor'

scrape_configs:
  # Prometheus self-monitoring
  # - job_name: 'prometheus'
  #   static_configs:
  #     - targets: ['localhost:9090']

  # Traffic Generator Application
  - job_name: 'traffic-generator'
    scrape_interval: 5s
    static_configs:
      - targets: ['traffic-generator:8000']
    metrics_path: '/metrics'
